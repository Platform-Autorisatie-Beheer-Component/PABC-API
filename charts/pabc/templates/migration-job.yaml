apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "pabc.fullname" . }}-migrations-{{ .Release.Revision }}
  labels:
    {{- include "pabc.labels" . | nindent 4 }}
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        {{- include "pabc.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "pabc.serviceAccountName" . }}
      {{- if .Values.migrations.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.migrations.nodeSelector | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}-migrations
        image: "{{ .Values.migrations.image.repository }}:{{ .Values.migrations.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.migrations.image.pullPolicy }}
        envFrom:
          - secretRef:
              name: {{ .Values.existingSecret | default (include "pabc.fullname" .) }}
          - configMapRef:
              name: {{ include "pabc.fullname" . }}
      {{- if .Values.migrations.extraVolumeMounts }}
        volumeMounts:
          {{- include "pabc.tplvalues.render" ( dict "value" .Values.migrations.extraVolumeMounts "context" $ ) | nindent 10 }}
      {{- end }}
      {{- if .Values.migrations.prefill.applications }}
        {{- if not .Values.migrations.extraVolumeMounts }}
        volumeMounts:
        {{- end }}
          - name: prefill
            mountPath: /app/prefill.json
            subPath: prefill.json
            readOnly: true
      {{- end }}
    {{- if or .Values.migrations.extraVolumes .Values.migrations.prefill.applications }}
      volumes:
        {{- if .Values.migrations.extraVolumes }}
        {{- include "pabc.tplvalues.render" ( dict "value" .Values.migrations.extraVolumes "context" $ ) | nindent 8 }}
        {{- end }}
        {{- if .Values.migrations.prefill.applications }}
        - name: prefill
          configMap:
            name: {{ include "pabc.fullname" . }}-prefill
        {{- end }}
    {{- end }}
    {{- if .Values.postgresql.enabled }}
      initContainers:
      - name: "{{ .Chart.Name }}-wait-for-postgresql"
        image: "{{ .Values.initContainers.waitFor.image.repository }}:{{ .Values.initContainers.waitFor.image.tag }}"
        imagePullPolicy: {{ .Values.initContainers.waitFor.image.pullPolicy }}
        args: 
        - "service"
        - "{{ .Release.Name }}-postgresql"
    {{- end }}
      restartPolicy: {{ .Values.migrations.restartPolicy }}
